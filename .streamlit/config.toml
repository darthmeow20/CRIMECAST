[theme]

# Dark OPS COMMAND theme (matches report_materials/screenshots reference)
base = "dark"

# Crimson accent
primaryColor = "#dc2626"

# Pure black main background
backgroundColor = "#0a0a0c"

# Sidebar / secondary panels
secondaryBackgroundColor = "#0c0c0f"

# Light text
textColor = "#e8e8ed"

font = "sans serif"
